# CIFAR-10 flow matching

Class-conditional image generation using rectified flow matching on CIFAR-10. Data-parallel only, the mesh shards on a single `data` axis.

Run via:

```shell
python main.py
```