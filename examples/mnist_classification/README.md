# MNIST classification

Minimal classification example using a small CNN trained with
softmax cross-entropy on MNIST. Data-parallel only, the mesh shards on a single `data` axis. Run via:

```shell
python main.py
```
